import type { NextPage } from 'next';
import styled from 'styled-components';

const Div = styled.div`
  font-weight: bold;
`;

const CompanySelect: NextPage = () => {
  return <Div>CompanySelect</Div>;
};

export default CompanySelect;
