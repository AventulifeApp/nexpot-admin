import type { NextPage } from 'next';
import styled from 'styled-components';

const Div = styled.div`
  font-weight: bold;
`;

const StoreUser: NextPage = () => {
  return <Div>StoreUser</Div>;
};

export default StoreUser;
